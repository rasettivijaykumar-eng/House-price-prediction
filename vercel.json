{
  "version": 2,
  "builds": [
    { "src": "frontend/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "^/(.*\\.(?:js|css|png|jpg|jpeg|svg|ico))$", "dest": "/frontend/$1" },
    { "src": "^/(.*)$", "dest": "/frontend/index.html" }
  ]
}
